class Ball {

   field int x, y; // screen location
   field int size; // size of ball
   field int xspeed, yspeed;

   /** Constructor */
   constructor Ball new(int Ax, int Ay, int Asize) {
      let x = Ax;
      let y = Ay;
      let size = Asize;
	  let xspeed = 1;
	  let yspeed = 1;
      do draw();
      return this;
   }
   
   /** Disposal */
   method void dispose() {
      do Memory.deAlloc(this);
      return;
   }
   
   /** Draw */
   method void draw() {
      do Screen.setColor(true);
      do Screen.drawCircle(x, y, x + size, y + size);
      return;
   }
   
   /** Move */
   method void move(){
		if(y <= 0 || (y + size) >= 254){ 
			let yspeed = multiply(y,-1);
		{
		if(x <= 2){
			//check if the player paddle touches the ball
			//otherwise increment opponent score
		}
		if((x+size) >= 252){
			//check if the opponent paddle touches the ball
			//otherwise increment player score
		}
		let y = y + yspeed;
   }
   
}